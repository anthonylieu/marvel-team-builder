<body>
    <section class="searchbar">
        <input type="text" />
        <button class="search"><img src="/images/ui_assets/search.png" alt="search" class="glass"/></button>
    </section>
    <main class="stat_wrapper align-baseline container">
        <div class="characters-container row row-cols-2 row-cols-md-3 row-cols-lg-6">
            {{#each characters as |character|}}
            <div class="character card bg-secondary col mb-4">
                <div class="card-body">
                    <h2 class="card-title">{{character.title}}</h2>
                    <img src="/images/{{character.image}}" alt="{{character.title}}" class="char-image img-fluid mb-2"/>
                    <div class="character-stats">
                        <div class="row">
                            <div class="col-6">
                                <div class="chart text-white">Intelligence: {{character.intelligence}}</div>
                                <div class="chart text-white">Strength: {{character.strength}}</div>
                                <div class="chart text-white">Speed: {{character.speed}}</div>
                            </div>
                            <div class="col-6">
                                <div class="chart text-white">Durability: {{character.durability}}</div>
                                <div class="chart text-white">Energy: {{character.energy}}</div>
                                <div class="chart text-white">Fighting Skills: {{character.fighting_skills}}</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </main>
    <script src="/js/team.js"></script>
    <script src="/js/search.js"></script>
</body>